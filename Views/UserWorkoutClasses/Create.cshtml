@model ProjektNET.Models.UserWorkoutClass

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>UserWorkoutClass</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div class="form-group">
                <label asp-for="UserId" class="control-label"></label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.UserId"></select>
                <span asp-validation-for="UserId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="WorkoutClassId" class="control-label"></label>
                <select asp-for="WorkoutClassId" class="form-control" asp-items="ViewBag.WorkoutClassId"></select>
                <span asp-validation-for="WorkoutClassId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">Create</button>
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
